# Core (required)
numpy>=1.24
flask>=3.0

# LLM providers (install at least one)
google-genai          # Gemini — recommended, has free tier
# groq                # Groq — fast free inference
# openai              # OpenAI
# anthropic           # Anthropic Claude

# Semantic embeddings (optional but recommended)
 sentence-transformers  # pip install sentence-transformers
#                        # Set RECALL_EMBEDDER=sentence-transformers in .env
